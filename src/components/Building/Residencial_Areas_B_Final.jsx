/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 ./public/models/Residencial_Areas.glb 
*/

import React, { useEffect, useMemo, useRef, useState } from "react";
import { useGLTF } from "@react-three/drei";
import { annotation3d, residenceTogglesAtom } from "../UI";
import { useAtom } from "jotai";

export const tipologiasResidencias = [
  "2BR_G_A",
  "2BR_G_B",
  "2BR_N1_A",
  "2BR_N1_B",
  "2BR_N2_A",
  "2BR_N2_B",
  "3BR_G_A",
  "3BR_G_B",
  "3BR_N1_A",
  "3BR_N1_B",
  "3BR_N2_A",
  "3BR_N2_B",
  "4BR_A",
  "4BR_B",
];

const ResidencialAreasB = (props) => {
  const { nodes, materials } = useGLTF("./models/Residencial_Areas_B.glb");

  const [annotation] = useAtom(annotation3d);
  const [residenceToggles] = useAtom(residenceTogglesAtom);

  //State for hover scaling
  const [hoveredMesh, setHoveredMesh] = useState(null);

  //Refs for each mesh
  const meshRefs = useRef({});

  //Refs for materials to handle opacity
  const materialRefs = useRef({});

  //Effect to handle opacity and color changes
  useEffect(() => {
    Object.keys(materialRefs.current).forEach((meshName) => {
      const material = materialRefs.current[meshName];
      if (material) {
        if (hoveredMesh === meshName) {
          material.opacity = 0.8;
          // material.emissive.set(0xffffff) // White emissive for bright white effect
          material.emissive.copy(material.originalEmissive);
          material.emissiveIntensity = 0.3;
          material.needsUpdate = true;
        } else {
          material.opacity = 0.45; // Restaurar la transparencia base de 0.75
          material.emissive.copy(material.originalEmissive); // Restore original emissive
          material.emissiveIntensity = 0;
          material.needsUpdate = true;
        }
        material.transparent = true;
      }
    });
  }, [hoveredMesh]);

  // // Handle hover events
  // const handlePointerOver = (meshName) => {

  // Handle hover events with stopPropagation to avoid bubbling into overlapped meshes
  const handlePointerEnter = (meshName, event) => {
    event.stopPropagation?.();

    setHoveredMesh(meshName);
    document.body.style.cursor = "pointer";
  };

  // const handlePointerOut = () => {

  const handlePointerLeave = (event) => {
    event.stopPropagation?.();

    setHoveredMesh(null);
    document.body.style.cursor = "default";
  };

  // Mapping de objetos a imagenes y titulos
  const objectMappings = {
    "2BR_G_B": {
      image: "./FP/2BR_B.jpg",
      title: "2 Bedrooms B Garden: 211.54 sqm",
    },
    "2BR_N1_B": {
      image: "./FP/2BR_B.jpg",
      title: "2 Bedrooms B L1: 211.54.00 sqm",
    },
    "2BR_N2_B": {
      image: "./FP/2BR_B.jpg",
      title: "2 Bedrooms B L2: 211.54.00 sqm",
    },
    "3BR_G_B": {
      image: "./FP/3BR_B.jpg",
      title: "3 Bedrooms B Garden: 215.71 sqm",
    },
    "3BR_N1_B": {
      image: "./FP/3BR_B.jpg",
      title: "3 Bedrooms B L1: 215.71 sqm",
    },
    "3BR_N2_B": {
      image: "./FP/3BR_B.jpg",
      title: "3 Bedrooms B L2: 215.71 sqm",
    },
    "4BR_B": { image: "./FP/4BR_B.jpg", title: "4 Bedrooms B: 428.86 sqm" },
  };

  // Handle click to active amenities camera
  const handleClick = (objectName, event) => {
    event?.stopPropagation?.();
    // setCameraMode(CameraModes.RESIDENCES);

    // Disparar evento para mostrar floating panel con informacion especifica del objeto
    const mapping = objectMappings[objectName];
    if (mapping) {
      // window.dispatchEvent(new CustomEvent('annotation-click', {
      window.dispatchEvent(
        new CustomEvent("mesh-click", {
          detail: {
            image: mapping.image,
            annotation: mapping.title,
            // meshName: objectName,
            // meshName: objectName,
            // source: 'mesh'
          },
        }),
      );
    }
  };

  // // Clone material to handle opacity and color changes
  // const getMaterial = (baseMaterial, meshName) => {

  // Clone material to handle opacity and allow optional color override per mesh
  const getMaterial = (baseMaterial, meshName, colorOverride) => {
    if (!materialRefs.current[meshName]) {
      const clonedMaterial = baseMaterial.clone();
      clonedMaterial.transparent = true;

      clonedMaterial.depthWrite = true;
      clonedMaterial.depthTest = true;

      clonedMaterial.opacity = 0.75; // Establecer transparencia de 0.75
      clonedMaterial.originalColor = clonedMaterial.color.clone(); // Store original color
      clonedMaterial.originalEmissive = clonedMaterial.emissive.clone(); //Store original emissive

      // Optional: override base color from the mesh config
      if (colorOverride) {
        clonedMaterial.color.set(colorOverride);
      }

      materialRefs.current[meshName] = clonedMaterial;
    }
    return materialRefs.current[meshName];
  };

  // useEffect(() => {
  //   setTipologyResidencias(tipologiasResidencias);
  // }, [tipologiasResidencias]);

  // const tipoCheck = (tipologia) => {
  //   return (
  //     cameraMode === CameraModes.RESIDENCES &&
  //     tipologyResidencias.includes(tipologia)
  //   );
  // };

  const tipoCheck = (tipologia, residenceType) => {
    if (annotation === "residences") {
      if (residenceType && residenceToggles[residenceType]) {
        return true;
      }
      return false;
    }
    return false;
  };

  return (
    <group {...props} dispose={null}>
      <mesh
        ref={(el) => (meshRefs.current["2BR_G_B"] = el)}
        geometry={nodes.R_2BR_G.geometry}
        // material={getMaterial(materials['Material_2.012'], '2BR_G_B', '#9C9284')}
        material={getMaterial(
          materials["Material_2.012"],
          "2BR_G_B",
          "#2E3641",
        )}
        visible={tipoCheck("2BR_G_B", "2 Bedroom Garden")}
        scale={hoveredMesh === "2BR_G_B" ? 1.003 : 1}
        // onPointerOver={() => handlePointerOver('2BR_G_B')}
        // onPointerOut={handlePointerOut}

        onPointerEnter={(event) => handlePointerEnter("2BR_G_B", event)}
        onPointerLeave={handlePointerLeave}
        onClick={(event) => handleClick("2BR_G_B", event)}
      >
        {/* <lineSegments scale={1.001}> */}

        <lineSegments scale={1.001} raycast={() => null}>
          <edgesGeometry args={[nodes.R_2BR_G.geometry, Math.PI]} />
          <lineBasicMaterial color="#C3C3C3" transparent opacity={0.9} />
        </lineSegments>
      </mesh>

      <mesh
        ref={(el) => (meshRefs.current["2BR_N1_B"] = el)}
        geometry={nodes.R_2BR_N1.geometry}
        // material={getMaterial(materials['Material_2.013'], '2BR_N1_B', '#67849A')}
        material={getMaterial(
          materials["Material_2.013"],
          "2BR_N1_B",
          "#2E3641",
        )}
        visible={tipoCheck("2BR_N1_B", "2 Bedroom")}
        scale={hoveredMesh === "2BR_N1_B" ? 1.003 : 1}
        // onPointerOver={() => handlePointerOver('2BR_N1_B')}
        // onPointerOut={handlePointerOut}

        onPointerEnter={(event) => handlePointerEnter("2BR_N1_B", event)}
        onPointerLeave={handlePointerLeave}
        onClick={(event) => handleClick("2BR_N1_B", event)}
      >
        {/* <lineSegments scale={1.001}> */}

        <lineSegments scale={1.001} raycast={() => null}>
          <edgesGeometry args={[nodes.R_2BR_N1.geometry, Math.PI]} />
          <lineBasicMaterial color="#C3C3C3" transparent opacity={0.9} />
        </lineSegments>
      </mesh>

      <mesh
        ref={(el) => (meshRefs.current["2BR_N2_B"] = el)}
        geometry={nodes.R_2BR_N2.geometry}
        // material={getMaterial(materials['Material_2.013'], '2BR_N2_B', '#67849A')}
        material={getMaterial(
          materials["Material_2.013"],
          "2BR_N2_B",
          "#2E3641",
        )}
        visible={tipoCheck("2BR_N2_B", "2 Bedroom")}
        scale={hoveredMesh === "2BR_N2_B" ? 1.003 : 1}
        // onPointerOver={() => handlePointerOver('2BR_N2_B')}
        // onPointerOut={handlePointerOut}

        onPointerEnter={(event) => handlePointerEnter("2BR_N2_B", event)}
        onPointerLeave={handlePointerLeave}
        onClick={(event) => handleClick("2BR_N2_B", event)}
      >
        {/* <lineSegments scale={1.001}> */}

        <lineSegments scale={1.001} raycast={() => null}>
          <edgesGeometry args={[nodes.R_2BR_N2.geometry, Math.PI]} />
          <lineBasicMaterial color="#C3C3C3" transparent opacity={0.9} />
        </lineSegments>
      </mesh>

      <mesh
        ref={(el) => (meshRefs.current["3BR_G_B"] = el)}
        geometry={nodes.R_3BR_G.geometry}
        // material={getMaterial(materials['Material_2.012'], '3BR_G_B', '#2F3640')}
        material={getMaterial(
          materials["Material_2.012"],
          "3BR_G_B",
          "#2E3641",
        )}
        visible={tipoCheck("3BR_G_B", "3 Bedroom Garden")}
        scale={hoveredMesh === "3BR_G_B" ? 1.003 : 1}
        // onPointerOver={() => handlePointerOver('3BR_G_B')}
        // onPointerOut={handlePointerOut}

        onPointerEnter={(event) => handlePointerEnter("3BR_G_B", event)}
        onPointerLeave={handlePointerLeave}
        onClick={(event) => handleClick("3BR_G_B", event)}
      >
        {/* <lineSegments scale={1.001}> */}

        <lineSegments scale={1.001} raycast={() => null}>
          <edgesGeometry args={[nodes.R_3BR_G.geometry, Math.PI]} />
          <lineBasicMaterial color="#C3C3C3" transparent opacity={0.9} />
        </lineSegments>
      </mesh>

      <mesh
        ref={(el) => (meshRefs.current["3BR_N1_B"] = el)}
        geometry={nodes.R_3BR_N1.geometry}
        // material={getMaterial(materials['Material_2.013'], '3BR_N1_B', '#DCE5EA')}
        material={getMaterial(
          materials["Material_2.013"],
          "3BR_N1_B",
          "#2E3641",
        )}
        visible={tipoCheck("3BR_N1_B", "3 Bedroom")}
        scale={hoveredMesh === "3BR_N1_B" ? 1.003 : 1}
        // onPointerOver={() => handlePointerOver('3BR_N1_B')}
        // onPointerOut={handlePointerOut}

        onPointerEnter={(event) => handlePointerEnter("3BR_N1_B", event)}
        onPointerLeave={handlePointerLeave}
        onClick={(event) => handleClick("3BR_N1_B", event)}
      >
        {/* <lineSegments scale={1.001}> */}

        <lineSegments scale={1.001} raycast={() => null}>
          <edgesGeometry args={[nodes.R_3BR_N1.geometry, Math.PI]} />
          <lineBasicMaterial color="#C3C3C3" transparent opacity={0.9} />
        </lineSegments>
      </mesh>

      <mesh
        ref={(el) => (meshRefs.current["3BR_N2_B"] = el)}
        geometry={nodes.R_3BR_N2.geometry}
        // material={getMaterial(materials['Material_2.013'], '3BR_N2_B', '#DCE5EA')}
        material={getMaterial(
          materials["Material_2.013"],
          "3BR_N2_B",
          "#2E3641",
        )}
        visible={tipoCheck("3BR_N2_B", "3 Bedroom")}
        scale={hoveredMesh === "3BR_N2_B" ? 1.003 : 1}
        // onPointerOver={() => handlePointerOver('3BR_N2_B')}
        // onPointerOut={handlePointerOut}

        onPointerEnter={(event) => handlePointerEnter("3BR_N2_B", event)}
        onPointerLeave={handlePointerLeave}
        onClick={(event) => handleClick("3BR_N2_B", event)}
      >
        {/* ★ EDGES (bordes externos sin inner edges) */}
        {/* <lineSegments scale={1.001}> */}

        <lineSegments scale={1.001} raycast={() => null}>
          <edgesGeometry args={[nodes.R_3BR_N2.geometry, Math.PI]} />
          <lineBasicMaterial color="#C3C3C3" transparent opacity={0.9} />
        </lineSegments>
      </mesh>

      <mesh
        ref={(el) => (meshRefs.current["4BR_B"] = el)}
        geometry={nodes.R_4BR.geometry}
        // material={getMaterial(materials['Material_2.012'], '4BR_B', '#E8E2DB')}
        material={getMaterial(materials["Material_2.012"], "4BR_B", "#2E3641")}
        visible={tipoCheck("4BR_B", "4 Bedroom")}
        scale={hoveredMesh === "4BR_B" ? 1.003 : 1}
        // onPointerOver={() => handlePointerOver('4BR_B')}
        // onPointerOut={handlePointerOut}

        onPointerEnter={(event) => handlePointerEnter("4BR_B", event)}
        onPointerLeave={handlePointerLeave}
        onClick={(event) => handleClick("4BR_B", event)}
      >
        {/* ★ EDGES (bordes externos sin inner edges) */}
        {/* <lineSegments scale={1.001}> */}

        <lineSegments scale={1.001} raycast={() => null}>
          <edgesGeometry args={[nodes.R_4BR.geometry, Math.PI]} />
          <lineBasicMaterial color="#C3C3C3" transparent opacity={0.9} />
        </lineSegments>
      </mesh>
    </group>
  );
};

useGLTF.preload("./models/Residencial_Areas_B.glb");

export default ResidencialAreasB;
